import React from "react";

const Header = () => {
    return (
        <header>
            <h1>React App: Adding/Deleting Courses</h1>
            <p>Add course item(s) using the input box below</p>
        </header>
    )
}

export default Header
